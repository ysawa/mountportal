- decorator = UserDecorator.new(@user)
.columns.small-12.large-6.panel.large-centered
  %h2= make_page_title('profiles', 'edit')
  = render 'profiles/form', user: @user

= content_for :title do
  #{make_page_title('profiles', 'edit')} | #{site_title}
